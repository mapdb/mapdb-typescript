// Code generated by mapdb-codegen. DO NOT EDIT.
import type { MapDbMutableBag } from "../api/index";

/**
 * Hash bag (multiset) for bigint values.
 * Tracks element occurrence counts using a Map<bigint, number>.
 */
export class BigIntHashBag implements MapDbMutableBag<bigint> {
  private counts: Map<bigint, number>;
  private _size: number;

  constructor() {
    this.counts = new Map<bigint, number>();
    this._size = 0;
  }

  /** Creates a new bag from an array of values. */
  static of(values: bigint[]): BigIntHashBag {
    const bag = new BigIntHashBag();
    for (const v of values) {
      bag.add(v);
    }
    return bag;
  }

  /** Adds a single occurrence of the value. */
  add(value: bigint): void {
    this.addOccurrences(value, 1);
  }

  /** Adds the specified number of occurrences of the value. */
  addOccurrences(value: bigint, occurrences: number): void {
    if (occurrences < 0) {
      throw new RangeError("Occurrences must not be negative");
    }
    if (occurrences === 0) return;
    const current = this.counts.get(value) ?? 0;
    this.counts.set(value, current + occurrences);
    this._size += occurrences;
  }

  /** Removes one occurrence of the value. Returns true if the value was present. */
  remove(value: bigint): boolean {
    const count = this.counts.get(value);
    if (count === undefined || count <= 0) return false;
    if (count === 1) {
      this.counts.delete(value);
    } else {
      this.counts.set(value, count - 1);
    }
    this._size--;
    return true;
  }

  /** Removes all occurrences of the value. Returns the previous count. */
  removeAll(value: bigint): number {
    const count = this.counts.get(value);
    if (count === undefined) return 0;
    this.counts.delete(value);
    this._size -= count;
    return count;
  }

  /** Removes the specified number of occurrences. Returns true if the value was present. */
  removeOccurrences(value: bigint, occurrences: number): boolean {
    if (occurrences < 0) {
      throw new RangeError("Occurrences must not be negative");
    }
    const current = this.counts.get(value);
    if (current === undefined) return false;
    if (occurrences >= current) {
      this.counts.delete(value);
      this._size -= current;
    } else {
      this.counts.set(value, current - occurrences);
      this._size -= occurrences;
    }
    return true;
  }

  /** Returns the number of occurrences of the value. */
  occurrencesOf(value: bigint): number {
    return this.counts.get(value) ?? 0;
  }

  /** Returns true if the bag contains at least one occurrence of the value. */
  contains(value: bigint): boolean {
    return this.counts.has(value);
  }

  /** Returns the total number of elements (including duplicates). */
  size(): number {
    return this._size;
  }

  /** Returns the number of distinct elements. */
  sizeDistinct(): number {
    return this.counts.size;
  }

  /** Returns true if the bag is empty. */
  isEmpty(): boolean {
    return this._size === 0;
  }

  /** Removes all elements. */
  clear(): void {
    this.counts.clear();
    this._size = 0;
  }

  /** Returns a new bag with elements satisfying the predicate. */
  select(predicate: (value: bigint) => boolean): BigIntHashBag {
    const result = new BigIntHashBag();
    for (const [value, count] of this.counts) {
      if (predicate(value)) {
        result.addOccurrences(value, count);
      }
    }
    return result;
  }

  /** Returns a new bag with elements NOT satisfying the predicate. */
  reject(predicate: (value: bigint) => boolean): BigIntHashBag {
    const result = new BigIntHashBag();
    for (const [value, count] of this.counts) {
      if (!predicate(value)) {
        result.addOccurrences(value, count);
      }
    }
    return result;
  }

  /** Calls the function for each element (including duplicates). */
  forEach(f: (value: bigint) => void): void {
    for (const [value, count] of this.counts) {
      for (let i = 0; i < count; i++) {
        f(value);
      }
    }
  }

  /** Returns a new array with the results of calling `f` on each element (including duplicates). */
  map<U>(f: (value: bigint) => U): U[] {
    const result: U[] = [];
    for (const [value, count] of this.counts) {
      for (let i = 0; i < count; i++) result.push(f(value));
    }
    return result;
  }

  /** Returns a new array with elements satisfying the predicate (including duplicates). */
  filter(predicate: (value: bigint) => boolean): bigint[] {
    const result: bigint[] = [];
    for (const [value, count] of this.counts) {
      if (predicate(value)) {
        for (let i = 0; i < count; i++) result.push(value);
      }
    }
    return result;
  }

  /** Returns the first element satisfying the predicate, or undefined. */
  find(predicate: (value: bigint) => boolean): bigint | undefined {
    for (const [value] of this.counts) {
      if (predicate(value)) return value;
    }
    return undefined;
  }

  /** Returns true if every distinct element satisfies the predicate. */
  every(predicate: (value: bigint) => boolean): boolean {
    for (const [value] of this.counts) {
      if (!predicate(value)) return false;
    }
    return true;
  }

  /** Returns true if at least one element satisfies the predicate. */
  some(predicate: (value: bigint) => boolean): boolean {
    for (const [value] of this.counts) {
      if (predicate(value)) return true;
    }
    return false;
  }

  /** Reduces the bag to a single value using the accumulator function (including duplicates). */
  reduce<U>(f: (acc: U, value: bigint) => U, initial: U): U {
    let acc = initial;
    for (const [value, count] of this.counts) {
      for (let i = 0; i < count; i++) acc = f(acc, value);
    }
    return acc;
  }

  /** Returns true if the bag contains the value. Alias for `contains`. */
  includes(value: bigint): boolean {
    return this.contains(value);
  }

  /** Makes the bag iterable with for-of loops. Yields all elements including duplicates. */
  *[Symbol.iterator](): IterableIterator<bigint> {
    for (const [value, count] of this.counts) {
      for (let i = 0; i < count; i++) yield value;
    }
  }

  /** Calls the function for each distinct element with its occurrence count. */
  forEachWithOccurrences(
    f: (value: bigint, occurrences: number) => void,
  ): void {
    for (const [value, count] of this.counts) {
      f(value, count);
    }
  }

  /** Yields all distinct elements with their occurrence counts as [value, count] tuples. */
  *entries(): Generator<[bigint, number]> {
    for (const [value, count] of this.counts) {
      yield [value, count];
    }
  }

  /** Returns all elements (including duplicates) as a plain array. */
  toArray(): bigint[] {
    const result: bigint[] = [];
    for (const [value, count] of this.counts) {
      for (let i = 0; i < count; i++) {
        result.push(value);
      }
    }
    return result;
  }

  /** Returns a string representation. */
  toString(): string {
    const parts: string[] = [];
    for (const [value, count] of this.counts) {
      parts.push(`${value}x${count}`);
    }
    return `{${parts.join(", ")}}`;
  }
}
