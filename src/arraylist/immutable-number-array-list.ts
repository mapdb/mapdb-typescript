// Code generated by mapdb-codegen. DO NOT EDIT.
import type { MapDbList } from "../api/index";
import { NumberArrayList } from "./number-array-list.js";

/**
 * Immutable array list holding number values.
 * Wraps a mutable NumberArrayList, exposing only read-only operations.
 */
export class ImmutableNumberArrayList implements MapDbList<number> {
  private readonly delegate: NumberArrayList;

  constructor(source: NumberArrayList) {
    // Copy all entries into a fresh mutable list so the caller cannot mutate our data.
    this.delegate = new NumberArrayList(source.size());
    for (let i = 0; i < source.size(); i++) {
      this.delegate.add(source.get(i));
    }
  }

  /** Creates an immutable list from an array of values. */
  static of(values: number[]): ImmutableNumberArrayList {
    return new ImmutableNumberArrayList(NumberArrayList.of(values));
  }

  /** Returns the value at the given index. Throws if out of bounds. */
  get(index: number): number {
    return this.delegate.get(index);
  }

  /** Returns the number of elements. */
  size(): number {
    return this.delegate.size();
  }

  /** Returns true if the list is empty. */
  isEmpty(): boolean {
    return this.delegate.isEmpty();
  }

  /** Returns true if the list contains the given value. */
  contains(value: number): boolean {
    return this.delegate.contains(value);
  }

  /** Returns the index of the first occurrence of the value, or -1 if not found. */
  indexOf(value: number): number {
    return this.delegate.indexOf(value);
  }

  /** Yields all elements in order. */
  *entries(): Generator<number> {
    yield* this.delegate.entries();
  }

  /** Calls the function for each element. */
  forEach(f: (value: number, index: number) => void): void {
    this.delegate.forEach(f);
  }

  /** Returns a new array with the results of calling `f` on each element. */
  map<U>(f: (value: number) => U): U[] {
    return this.delegate.map(f);
  }

  /** Returns a new array with elements satisfying the predicate. */
  filter(predicate: (value: number) => boolean): number[] {
    return this.delegate.filter(predicate);
  }

  /** Returns the first element satisfying the predicate, or undefined. Alias for `detect`. */
  find(predicate: (value: number) => boolean): number | undefined {
    return this.delegate.find(predicate);
  }

  /** Returns true if every element satisfies the predicate. Alias for `allSatisfy`. */
  every(predicate: (value: number) => boolean): boolean {
    return this.delegate.every(predicate);
  }

  /** Returns true if at least one element satisfies the predicate. Alias for `anySatisfy`. */
  some(predicate: (value: number) => boolean): boolean {
    return this.delegate.some(predicate);
  }

  /** Reduces the list to a single value using the accumulator function. */
  reduce<U>(f: (acc: U, value: number) => U, initial: U): U {
    return this.delegate.reduce(f, initial);
  }

  /** Returns true if the list contains the value. Alias for `contains`. */
  includes(value: number): boolean {
    return this.delegate.includes(value);
  }

  /** Makes the list iterable with for-of loops. */
  *[Symbol.iterator](): IterableIterator<number> {
    yield* this.delegate[Symbol.iterator]();
  }

  /** Returns a new immutable list with elements satisfying the predicate. */
  select(predicate: (value: number) => boolean): ImmutableNumberArrayList {
    return new ImmutableNumberArrayList(this.delegate.select(predicate));
  }

  /** Returns a new immutable list with elements NOT satisfying the predicate. */
  reject(predicate: (value: number) => boolean): ImmutableNumberArrayList {
    return new ImmutableNumberArrayList(this.delegate.reject(predicate));
  }

  /** Returns the first element satisfying the predicate, or undefined. */
  detect(predicate: (value: number) => boolean): number | undefined {
    return this.delegate.detect(predicate);
  }

  /** Returns true if any element satisfies the predicate. */
  anySatisfy(predicate: (value: number) => boolean): boolean {
    return this.delegate.anySatisfy(predicate);
  }

  /** Returns true if all elements satisfy the predicate. */
  allSatisfy(predicate: (value: number) => boolean): boolean {
    return this.delegate.allSatisfy(predicate);
  }

  /** Returns the number of elements satisfying the predicate. */
  count(predicate: (value: number) => boolean): number {
    return this.delegate.count(predicate);
  }

  /** Returns the sum of all elements. */
  sum(): number {
    return this.delegate.sum();
  }

  /** Returns the minimum element, or undefined if empty. */
  min(): number | undefined {
    return this.delegate.min();
  }

  /** Returns the maximum element, or undefined if empty. */
  max(): number | undefined {
    return this.delegate.max();
  }

  /** Returns the elements as a plain array. */
  toArray(): number[] {
    return this.delegate.toArray();
  }

  /** Returns a new mutable copy of this list. */
  toMutable(): NumberArrayList {
    return NumberArrayList.of(this.delegate.toArray());
  }

  /** Returns a string representation. */
  toString(): string {
    return this.delegate.toString();
  }
}
