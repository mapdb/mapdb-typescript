// Code generated by mapdb-codegen. DO NOT EDIT.

import { BigInt64ArrayStack } from "./bigint64-array-stack";

/**
 * Immutable stack (LIFO) backed by BigInt64Array.
 * Elements: 8 bytes each, contiguous memory.
 * Construct via static of(values) or fromMutable(mutable).
 * Mutations create new instances; select/reject return MUTABLE.
 */
export class ImmutableBigInt64ArrayStack {
  private data: BigInt64Array;
  private _size: number;

  /** Creates an immutable stack from an array of values (defensive copy). Last element = top. */
  static of(values: bigint[]): ImmutableBigInt64ArrayStack {
    const size = values.length;
    const data = new BigInt64Array(size);
    data.set(values);
    return new ImmutableBigInt64ArrayStack(data, size);
  }

  /** Creates an immutable copy from a mutable stack (defensive copy). */
  static fromMutable(mutable: BigInt64ArrayStack): ImmutableBigInt64ArrayStack {
    const arr = mutable.toArray();
    const data = new BigInt64Array(arr.length);
    data.set(arr);
    return new ImmutableBigInt64ArrayStack(data, arr.length);
  }

  private constructor(data: BigInt64Array, size: number) {
    this.data = data;
    this._size = size;
  }

  /** Returns the top element without removing it. Throws if empty. */
  peek(): bigint {
    if (this._size === 0) {
      throw new Error("Stack is empty");
    }
    return this.data[this._size - 1];
  }

  /** Returns the element at the given distance from the top (0 = top). Throws if out of bounds. */
  peekAt(index: number): bigint {
    if (index < 0 || index >= this._size) {
      throw new RangeError(
        `Index ${index} out of bounds for stack size ${this._size}`,
      );
    }
    return this.data[this._size - 1 - index];
  }

  /** Returns the number of elements. */
  size(): number {
    return this._size;
  }

  /** Returns true if the stack is empty. */
  isEmpty(): boolean {
    return this._size === 0;
  }

  /** Returns true if the stack contains the given value. */
  contains(value: bigint): boolean {
    for (let i = 0; i < this._size; i++) {
      if (Object.is(this.data[i], value)) return true;
    }
    return false;
  }

  /** Alias for contains. */
  includes(value: bigint): boolean {
    return this.contains(value);
  }

  /** Returns a new MUTABLE stack with elements satisfying the predicate. */
  select(predicate: (value: bigint) => boolean): BigInt64ArrayStack {
    const result = new BigInt64ArrayStack();
    for (let i = 0; i < this._size; i++) {
      if (predicate(this.data[i])) {
        result.push(this.data[i]);
      }
    }
    return result;
  }

  /** Returns a new MUTABLE stack with elements NOT satisfying the predicate. */
  reject(predicate: (value: bigint) => boolean): BigInt64ArrayStack {
    const result = new BigInt64ArrayStack();
    for (let i = 0; i < this._size; i++) {
      if (!predicate(this.data[i])) {
        result.push(this.data[i]);
      }
    }
    return result;
  }

  /** Returns the first element (from top) satisfying the predicate, or undefined. */
  find(predicate: (value: bigint) => boolean): bigint | undefined {
    for (let i = this._size - 1; i >= 0; i--) {
      if (predicate(this.data[i])) return this.data[i];
    }
    return undefined;
  }

  /** Returns true if every element satisfies the predicate. */
  every(predicate: (value: bigint) => boolean): boolean {
    for (let i = 0; i < this._size; i++) {
      if (!predicate(this.data[i])) return false;
    }
    return true;
  }

  /** Returns true if at least one element satisfies the predicate. */
  some(predicate: (value: bigint) => boolean): boolean {
    for (let i = 0; i < this._size; i++) {
      if (predicate(this.data[i])) return true;
    }
    return false;
  }

  /** Returns true if any element satisfies the predicate. */
  anySatisfy(predicate: (value: bigint) => boolean): boolean {
    return this.some(predicate);
  }

  /** Returns true if all elements satisfy the predicate. */
  allSatisfy(predicate: (value: bigint) => boolean): boolean {
    return this.every(predicate);
  }

  /** Returns the number of elements satisfying the predicate. */
  count(predicate: (value: bigint) => boolean): number {
    let c = 0;
    for (let i = 0; i < this._size; i++) {
      if (predicate(this.data[i])) c++;
    }
    return c;
  }

  /** Returns the sum of all elements. */
  sum(): bigint {
    let s: bigint = 0n as bigint;
    for (let i = 0; i < this._size; i++) {
      s = ((s as any) + this.data[i]) as any as bigint;
    }
    return s;
  }

  /** Returns the minimum element, or undefined if empty. */
  min(): bigint | undefined {
    if (this._size === 0) return undefined;
    let m = this.data[0];
    for (let i = 1; i < this._size; i++) {
      if (this.data[i] < m) m = this.data[i];
    }
    return m;
  }

  /** Returns the maximum element, or undefined if empty. */
  max(): bigint | undefined {
    if (this._size === 0) return undefined;
    let m = this.data[0];
    for (let i = 1; i < this._size; i++) {
      if (this.data[i] > m) m = this.data[i];
    }
    return m;
  }

  /** Yields all values from top to bottom. */
  *values(): Generator<bigint> {
    for (let i = this._size - 1; i >= 0; i--) {
      yield this.data[i];
    }
  }

  /** Makes the stack iterable with for-of loops. Yields top to bottom. */
  *[Symbol.iterator](): IterableIterator<bigint> {
    for (let i = this._size - 1; i >= 0; i--) {
      yield this.data[i];
    }
  }

  /** Returns all elements as a new array (top element first). */
  toArray(): bigint[] {
    const result: bigint[] = [];
    for (let i = this._size - 1; i >= 0; i--) {
      result.push(this.data[i]);
    }
    return result;
  }

  /** Returns a mutable copy of this immutable stack. */
  toMutable(): BigInt64ArrayStack {
    return BigInt64ArrayStack.fromImmutable(this);
  }

  /** Calls the function for each element from top to bottom. */
  forEach(f: (value: bigint) => void): void {
    for (let i = this._size - 1; i >= 0; i--) {
      f(this.data[i]);
    }
  }

  /** Returns a new array with the results of calling f on each element (top to bottom). */
  map<U>(f: (value: bigint) => U): U[] {
    const result: U[] = [];
    for (let i = this._size - 1; i >= 0; i--) {
      result.push(f(this.data[i]));
    }
    return result;
  }

  /** Returns a new array with elements satisfying the predicate (top to bottom). */
  filter(predicate: (value: bigint) => boolean): bigint[] {
    const result: bigint[] = [];
    for (let i = this._size - 1; i >= 0; i--) {
      if (predicate(this.data[i])) result.push(this.data[i]);
    }
    return result;
  }

  /** Reduces the stack to a single value using the accumulator function (top to bottom). */
  reduce<U>(f: (acc: U, value: bigint) => U, initial: U): U {
    let acc = initial;
    for (let i = this._size - 1; i >= 0; i--) {
      acc = f(acc, this.data[i]);
    }
    return acc;
  }

  /** Returns a string representation (top element first). */
  toString(): string {
    const parts: string[] = [];
    for (let i = this._size - 1; i >= 0; i--) {
      parts.push(`${this.data[i]}`);
    }
    return `[${parts.join(", ")}]`;
  }

  /** Memory used by the backing TypedArray in bytes. */
  memoryBytes(): number {
    return this.data.byteLength;
  }
}
