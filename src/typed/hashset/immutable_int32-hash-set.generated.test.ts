// Code generated by mapdb-codegen. DO NOT EDIT.

import { describe, it, expect } from "vitest";
import { ImmutableInt32HashSet } from "./immutable_int32-hash-set.js";
import { Int32HashSet } from "./int32-hash-set.js";

describe("ImmutableInt32HashSet generated", () => {
  it("static of creates immutable set", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3]);
    expect(set.size()).toBe(3);
    expect(set.contains(1)).toBe(true);
    expect(set.contains(3)).toBe(true);
  });

  it("fromMutable creates defensive copy", () => {
    const mutable = new Int32HashSet();
    mutable.add(1);
    mutable.add(2);
    const imm = ImmutableInt32HashSet.fromMutable(mutable);
    mutable.add(3);
    expect(imm.size()).toBe(2);
    expect(mutable.size()).toBe(3);
  });

  it("contains and includes", () => {
    const set = ImmutableInt32HashSet.of([1, 2]);
    expect(set.contains(1)).toBe(true);
    expect(set.contains(2)).toBe(true);
    expect(set.contains(99)).toBe(false);
    expect(set.includes(1)).toBe(true);
  });

  it("size and isEmpty", () => {
    const empty = ImmutableInt32HashSet.of([]);
    expect(empty.size()).toBe(0);
    expect(empty.isEmpty()).toBe(true);
    const nonEmpty = ImmutableInt32HashSet.of([1]);
    expect(nonEmpty.size()).toBe(1);
    expect(nonEmpty.isEmpty()).toBe(false);
  });

  it("select returns MUTABLE", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3, 4, 5]);
    const result = set.select((v) => v > 3);
    expect(typeof result.add).toBe("function");
    expect(result.size()).toBe(2);
  });

  it("reject returns MUTABLE", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3, 4, 5]);
    const result = set.reject((v) => v > 3);
    expect(typeof result.add).toBe("function");
    expect(result.size()).toBe(3);
  });

  it("anySatisfy / allSatisfy", () => {
    const set = ImmutableInt32HashSet.of([1, 2]);
    expect(set.anySatisfy((v) => v === 2)).toBe(true);
    expect(set.anySatisfy((v) => v === 99)).toBe(false);
    expect(set.allSatisfy((v) => v > 0)).toBe(true);
  });

  it("noneSatisfy", () => {
    const set = ImmutableInt32HashSet.of([1, 2]);
    expect(set.noneSatisfy((v) => v === 99)).toBe(true);
    expect(set.noneSatisfy((v) => v === 1)).toBe(false);
  });

  it("count", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3, 4, 5]);
    expect(set.count((v) => v > 3)).toBe(2);
  });

  it("toArray creates copy", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3]);
    const arr = set.toArray();
    // Modify the array - original should be unaffected (but we can't easily test this for sets)
    expect(arr.length).toBe(3);
  });

  it("toMutable round-trip", () => {
    const original = ImmutableInt32HashSet.of([1, 2, 3]);
    const mutable = original.toMutable();
    mutable.add(4);
    expect(mutable.size()).toBe(4);
    expect(original.size()).toBe(3);
  });

  it("forEach", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3]);
    const collected: number[] = [];
    set.forEach((v) => collected.push(v));
    expect(collected.length).toBe(3);
    expect(collected.includes(1)).toBe(true);
  });

  it("map", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3]);
    const result = set.map((v) => v * 2);
    expect(result.length).toBe(3);
  });

  it("filter", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3, 4]);
    const result = set.filter((v) => v <= 3);
    expect(result.length).toBe(3);
  });

  it("reduce", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3]);
    const sum = set.reduce((acc, v) => acc + v, 0);
    expect(sum).toBe(1 + 2 + 3);
  });

  it("values iterator", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3]);
    const collected: number[] = [];
    for (const v of set.values()) {
      collected.push(v);
    }
    expect(collected.length).toBe(3);
  });

  it("Symbol.iterator", () => {
    const set = ImmutableInt32HashSet.of([1, 2]);
    const collected: number[] = [];
    for (const v of set) {
      collected.push(v);
    }
    expect(collected.length).toBe(2);
  });

  it("toString", () => {
    const set = ImmutableInt32HashSet.of([1, 2]);
    const str = set.toString();
    expect(str).not.toBe("");
  });

  it("memoryBytes", () => {
    const set = ImmutableInt32HashSet.of([1, 2, 3]);
    // At least 3 elements * (byteSize + 1 for occupied) bytes
    expect(set.memoryBytes()).toBeGreaterThanOrEqual(3 * (4 + 1));
  });

  it("defensive copy - modifying source array doesn't affect immutable", () => {
    const source = [1, 2, 3];
    const set = ImmutableInt32HashSet.of(source);
    source[0] = 99 as any;
    expect(set.contains(1)).toBe(true);
    expect(set.contains(99)).toBe(false);
  });

  // Verify mutators are not available
  it("has no add method", () => {
    const set = ImmutableInt32HashSet.of([1]);
    // @ts-expect-error - add should not exist on immutable
    expect(set.add).toBeUndefined();
  });

  it("has no remove method", () => {
    const set = ImmutableInt32HashSet.of([1]);
    // @ts-expect-error - remove should not exist on immutable
    expect(set.remove).toBeUndefined();
  });
});
