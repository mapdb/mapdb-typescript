// Code generated by mapdb-codegen. DO NOT EDIT.

import { describe, it, expect } from "vitest";
import { BigIntArrayStack } from "./bigint-array-stack.js";
import { ImmutableBigIntArrayStack } from "./immutable-bigint-array-stack.js";

describe("ImmutableBigIntArrayStack generated", () => {
  it("peek and size", () => {
    const im = ImmutableBigIntArrayStack.of([1n, 2n]);
    expect(im.size()).toBe(2);
    expect(im.peek()).toBe(2n);
  });
  it("push returns new stack", () => {
    const im = ImmutableBigIntArrayStack.of([1n]);
    const im2 = im.push(2n);
    expect(im.size()).toBe(1);
    expect(im2.size()).toBe(2);
  });
  it("pop returns new stack and value", () => {
    const im = ImmutableBigIntArrayStack.of([1n, 2n]);
    const [im2, val] = im.pop();
    expect(val).toBe(2n);
    expect(im2.size()).toBe(1);
    expect(im.size()).toBe(2);
  });
  it("toMutable does not affect immutable", () => {
    const im = ImmutableBigIntArrayStack.of([1n]);
    im.toMutable().push(2n);
    expect(im.size()).toBe(1);
  });
});
